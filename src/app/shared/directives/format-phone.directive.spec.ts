import { FormatPhoneDirective } from './format-phone.directive';

describe('FormatPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
