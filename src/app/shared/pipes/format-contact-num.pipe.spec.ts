import { FormatContactNumPipe } from './format-contact-num.pipe';

describe('FormatContactNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatContactNumPipe();
    expect(pipe).toBeTruthy();
  });
});
